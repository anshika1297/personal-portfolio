import Typed from 'typed.js';

var options = {
  strings: ["I'm a Mobile App Developer", 'I Build Mobile Apps', 'I am an Avid Reader'],
  typeSpeed: 120,
  backSpeed: 50,
  backDelay: 1500,
  loop: true
};

var typed = new Typed('#headline', options);
